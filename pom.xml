
<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bungeecookie.sportspigot</groupId>
    <artifactId>SportSpigot-parent</artifactId>
    <version>dev-1.10-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>SportSpigot (Parent)</name>

    <build>
        <defaultGoal>install</defaultGoal>
        <finalName>SportSpigot</finalName>
        <plugins>
            <!--plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin-->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.3</version>
                <executions>
                    <!--execution>
                        <id>deploy</id>
                        <phase>deploy</phase>
                        <configuration>
                            <tasks>
                                <taskdef resource="net/sf/antcontrib/antlib.xml" classpathref="maven.plugin.classpath" />
                                <if>
                                    <and>
                                    <not><isset property="isBuildServer"/></not>
                                    <not><equals arg1="${project.artifactId}" arg2="empirecraft-parent" /></not>
                                    </and>
                                    <then>
                                        <exec executable="mvn">
                                            <arg value="deploy:deploy-file"/>
                                            <arg value="-DgroupId=com.empireminecraft" />
                                            <arg value="-DartifactId=${project.artifactId}" />
                                            <arg value="-Dversion=dev-SNAPSHOT" />
                                            <arg value="-Dpackaging=jar" />
                                            <arg value="-DrepositoryId=empireminecraft" />
                                            <arg value="-Durl=${repo}" />
                                            <arg value="-Dfile=${project.build.directory}/${project.artifactId}.jar" />
                                        </exec>
                                    </then>
                                </if>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution-->
                    <execution>
                        <id>install</id>
                        <phase>install</phase>
                        <configuration>
                            <tasks>
                                <taskdef resource="net/sf/antcontrib/antlib.xml" classpathref="maven.plugin.classpath" />
                                <if>
                                    <and>
                                    <not><isset property="isBuildServer"/></not>
                                    <equals arg1="${project.artifactId}" arg2="empirecraft" />
                                    </and>
                                    <then>
                                        <copy tofile="${project.build.directory}/../../../EMC-Test-Server/minecraft_server.jar">
                                            <fileset dir="${project.build.directory}" includes="${project.artifactId}.jar" />
                                        </copy>
                                        <copy tofile="${project.build.directory}/../../../EMC-Core/server/minecraft_server.jar">
                                            <fileset dir="${project.build.directory}" includes="${project.artifactId}.jar" />
                                        </copy>
                                    </then>
                                </if>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>1.0b3</version>
                        <exclusions>
                            <exclusion>
                                <groupId>ant</groupId>
                                <artifactId>ant</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                    <dependency>
                        <groupId>ant</groupId>
                        <artifactId>ant-nodeps</artifactId>
                        <version>1.6.5</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
    <modules>
        <module>SportSpigot-API</module>
        <module>SportSpigot-Server</module>
    </modules>
</project>
